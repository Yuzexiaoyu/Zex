# 桌面歌词第一版复盘:不足之处与优化方案

> 回滚背景:第一版实装后用户报告「完全无法操控」,已 git 回滚到存档点。
> 本文是纯复盘——按严重度列出缺陷(含「无法操控」的根因定位),以及重做时的完整优化方案。

## 一、「完全无法操控」根因定位(按可能性排序)

### P0-1 透明置顶窗口 = 隐形挡板(最可能)

**机制**:
- 窗口 `visible(false)` 建好后**立即 `show()`**;而穿透是**前端 React mount 后才调** `setIgnoreCursorEvents(true)`。
- WebView 加载 + JS 执行有几百 ms 到几秒的窗口期,期间窗口**可见但不穿透**。
- 若 `setIgnoreCursorEvents` 调用失败(权限名错误、IPC 异常,代码里是 `void` 调用、**异常被静默吞掉**),穿透**永远不生效**,但窗口透明——**一块看不见的 680×200 置顶挡板**。
- ZEX 主窗口**全屏**(tauri.conf.json `fullscreen: true`),歌词窗口默认位置 `center` = **正好盖在全屏主窗口正中央**。

**结果**:鼠标点哪里都被透明窗口吃掉,ZEX 完全无法操作 → 只能杀进程。与用户报告完全吻合。

### P0-2 窗口抢焦点(手柄/键盘焦点丢失)

**机制**:
- 实现只用了 `.focused(false)`(抄 tray-menu 先例,但托盘窗口建完**不 show**,右键才 show;**歌词窗口建完立即 show**)。
- 漏了 `.focusable(false)`(Windows 上对应 `WS_EX_NOACTIVATE`)——`show()` 一个新窗口 Windows 默认激活它 → **全屏 ZEX 主窗口失焦**。
- ZEX 的手柄导航(核心交互)基于 gilrs 全局输入 + 窗口焦点分发,失焦后按键全部落空。

**结果**:键盘/手柄「完全无法操控」,与 P0-1 叠加 = 双保险地卡死用户。

### P0-3 无「穿透就绪」握手协议

- 没有「穿透成功后才显示窗口」的机制,也没有失败兜底(隐藏窗口 + 提示)。
- 上述两个根因共同点:**窗口可见性没有与「安全状态」绑定**。

## 二、其余设计/实现缺陷(按严重度)

### P1-1 reader 线程做文件 IO(attach 阻塞事件循环)

`rotate_music_to` 和 reader 开头都在**事件循环线程**内同步执行 `Probe::open + read`(60MB flac 标签解析,10~100ms+,慢盘更久)+ 写文件 + IPC。

- 期间 reader 不读管道 → time-pos 事件积压 → 记账的 `TICK_GAP_MAX`(2s)逻辑判定停顿,**丢观看/收听时长**;
- 磁盘极慢时 mpv 事件积压到管道缓冲满,mpv 阻塞在写事件上,整个播放链路卡住(理论极限,但设计不该有这个可能)。

**关键事实**:`send_to_session` 是「每次新连接」模式,谁调都行——attach 完全不必在 reader 线程做,可以放到独立线程/预提取。

### P1-2 运行时 `sub-add` 的激活行为未验证

- 实测过的是**命令行** `--sub-file`(文件加载时作为字幕轨);**运行中 `sub-add` + select 在 `vid=no` 无窗口会话上的行为**(字幕轨是否激活、sub-text 是否推送)没测过。
- 若轨道不激活 → 歌词不显示(功能失效,验收清单也没覆盖)。

### P1-3 歌词窗口无「关闭」退出路径

- 穿透态点不到;调整态才能点,但没有关闭按钮。用户误开后只能去播放条关——**被挡板挡住时无路可退**。
- 需要:调整态显示「关闭」按钮(emit 通知主窗口关窗)。

### P1-4 歌词行事件无去重

- mpv 在 seek/暂停恢复时可能重推同一行 → 前端 `setLine` 高频重渲染。加「同 track 同行不重复 emit」或前端去重。

### P1-5 测试数据造假,真实路径未验证

- 用户库 175 首 flac **全部没有内嵌歌词**,测试文件是我用 python 手工注入的 Vorbis comment——格式与真实文件(如 Picard/foobar 写的)可能有差异,`lofty` 的真实读取路径(比如 `LYRICS` 小写键、`[ti:]` 元数据混排)没有验证。
- 事实上原文件自带一个 `Lyrics`(小写)键且是 lrc 格式——**用户库其实有带歌词的文件**,第一版扫描脚本的匹配逻辑有大小写问题没发现它。重做时先用真实文件验证。

## 三、优化方案(重做时按此执行)

### A. 窗口安全协议(必做,解决 P0)

```
建窗(visible=false + focusable(false) + focused(false) + 尺寸收敛到 640x120)
   │
   ▼ 前端 mount
setIgnoreCursorEvents(true) 成功?
   ├─ 否 → 不显示窗口,invoke('lyrics_window_ready', false) → 主窗口提示「歌词窗口开启失败」
   └─ 是 → invoke('lyrics_window_ready', true) → 后端才 show()
```

1. **穿透先于可见**:窗口永远以 `visible(false)` 创建;前端确认穿透生效后回执,后端收到回执才 `show()`。失败路径:不显示 + 主窗口 toast。
2. **`.focusable(false)` 必须**(WS_EX_NOACTIVATE),与 `.focused(false)` 并用;验收项:**show 前后主窗口 `isFocused()` 必须保持 true**。
3. **默认位置**:屏幕底部中央(主窗口 work area 下方 20%),**不居中**;只记忆用户拖动过的位置。
4. **调整态加「关闭」按钮**(发射事件让主窗口关窗),穿透态无关闭(状态不漂移原则保留)。

### B. 时序重构(解决 P1-1)

5. **attach 移出 reader 线程**:`rotate_music_to` 只登记「当前曲目已变」,提取 + 写文件 + sub-add 放到独立线程(`std::thread::spawn`,或复用现有「预提取」的异步模式)。`send_to_session` 线程安全(每次新连接),谁发都行。
6. 或更简单:播放前在 command 线程异步预提取当前曲目;换曲时只提取「下一首」提前缓存——但第一版复杂度优先,独立线程即可。

### C. 行为补强(解决 P1-2/3/4)

7. **先实测 sub-add 行为再定时间轴方案**:造真实带歌词文件(用 mutagen 或下载),验证 `sub-add select` 在 `vid=no` 下的激活与推送;若不可靠,退回「ZEX 自解析 lrc + time-pos 驱动」(前端二分查行,复杂度可接受)。
8. **行事件去重**:后端记住最后 emit 的 (track_id, line),相同不重发。
9. **「暂无歌词」区分「加载中」**:收到 music-track-changed 后先显示「…」,收到空行再显示「暂无歌词」。

### D. 验证补强(解决 P1-5)

10. 测试数据用**真实带内嵌歌词文件**:
    - 用户库原文件带 `Lyrics` 小写键(已确认),先验证 lofty 能否读它;
    - 安装 mutagen(`pip install mutagen`)造标准文件,覆盖 `LYRICS`/`Lyrics` 两种键。
11. 验收清单新增(回归必测):
    - **焦点回归**:歌词窗口打开后,主窗口仍可用键盘/手柄导航(show 前后 `isFocused` 断言)
    - **穿透回归**:窗口打开后,点击主窗口任意按钮均正常
    - **失败路径**:穿透 API 不可用时窗口不显示且主窗口有提示
    - **全屏场景**:ZEX 全屏时打开歌词窗口,主窗口操作不受任何影响

## 四、范围建议(重做时)

- 保持 MVP 范围(内嵌歌词 + 窗口 + 播放条按钮),但**按 A→B→C→D 顺序**实施,每步都编译+回归后再进下一步。
- 窗口安全协议(A)是本功能成立的前提,单独作为第一个提交点。
